# Prometheus Monitor Service (Metrics)
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    control-plane: eevee
    app.kubernetes.io/name: eevee
    app.kubernetes.io/managed-by: kustomize
  name: eevee-operator-metrics-monitor
  namespace: eevee-system
spec:
  endpoints:
    - path: /metrics
      port: http # Ensure this is the name of the port that exposes HTTPS metrics
      scheme: http
  selector:
    matchLabels:
      control-plane: eevee
